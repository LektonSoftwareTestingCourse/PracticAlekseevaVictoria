# Тест дизайн
## Authorization Service
### Классы эквивалентности

**1. Статус карты**

Позитивный класс:
- ACTIVE - карта активна

Негативные классы:
- INACTIVE - неактивная карта, система возвращает DECLINED с причиной CARD_INACTIVE
- BLOCKED - заблокированная карта, система возвращает DECLINED с причиной CARD_BLOCKED
- EXPIRED - карта с истекшим сроком действия, система возвращает DECLINED

**2. Наличие карты**

Позитивный класс:
- Карта существует в системе - обработка транзакции продолжается

Негативный класс:
- Карта отсутствует в системе - система возвращает DECLINED

**3. Срок действия карты**

Позитивный класс:
- expiryDate не истек - проверка продолжается

Негативный класс:
- expiryDate < текущей даты - система возвращает DECLINED

**4. Дневной лимит**

Позитивный класс:
- Сумма текущей транзакции вместе с уже использованной суммой < dailyLimit.

Негативный класс:
- Сумма текущей транзакции вместе с уже использованной суммой >= dailyLimit. Система возвращает DECLINED

**5. Месячный лимит**

Позитивный класс:
- Сумма операции вместе с текущим использованием лимита < monthlyLimit.

Негативный класс:
- Сумма операции >= monthlyLimit. Система возвращает DECLINED

**6.Доступный баланс**

Позитивный класс:
- Сумма транзакции <= availableBalance. Авторизация может быть выполнена.

Негативный класс:
- Сумма транзакции > availableBalance. Система возвращает DECLINED

### Анализ граничных значений

**1. DailyLimit**

Проверяется граница между допустимой и недопустимой суммой операций за день

Граница: сумма всех одобренных операций за день + текущая транзакция = dailyLimit.

Тестовые значения:

- OFF (значение ниже границы): dailyLimit-1. Успех
- ON (значение на границе): dailyLimit. Успех
- OFF (значение выше границы): dailyLimit+1. Отказ

**2. MonthlyLimit**

Проверяется граница между допустимой и недопустимой суммой операций за месяц

Граница: сумма всех одобренных операций за месяц + текущая транзакция = monthlyLimit.

Тестовые значения:

- OFF (значение ниже границы): monthlyLimit-1. Успех
- ON (значение на границе): monthlyLimit. Успех
- OFF (значение выше границы): monthlyLimit+1. Отказ

**3. AvailableBalance**

Проверяется граница между достаточным и недостаточным балансом карты.

Граница: сумма транзакции = availableBalance

Тестовые значения:

- OFF (значение ниже границы): availableBalance-1. Успех
- ON (значение на границе): availableBalance. Успех
-  OFF (значение выше границы): availableBalance+1. Отказ

**3. ExpiryDate**

Проверяется граница срока действия карты.

Граница: текущая дата окончания действия карты.

Тестовые значения:

- expiryDate > текущей - успех
- expiryDate = текущей - успех
- expiryDate < текущей - отказ

### Тест-кейсы

| Поле | Значение |
|-|-|
| **ID** | TC-01 |
| **Название** | Успешная авторизация активной карты |
| **Связанное требование** | Authorization - при валидных данных карта проходит проверки статуса, срока действия, лимитов и баланса, после чего транзакция получает статус APPROVED |
| **Вид** | Позитивный |
| **Предусловия** | Card Management Service доступен; существует карта со статусом ACTIVE; срок действия карты не истек; лимиты не превышены; баланс достаточный |
| **Тестовые данные** | PAN существующей активной карты; amount = 1000.00; availableBalance >= 1000.00 |
| **Шаги** | 1. Отправить запрос авторизации транзакции 2. Получить ответ Authorization Service. |
| **Ожидаемый результат** | Транзакция одобрена; средства зарезервированы через Card Management Service |

| Поле | Значение |
|-|-|
| **ID** | TC-02 |
| **Название** | Авторизация карты, отсутствующей в системе |
| **Связанное требование** | Authorization - при отсутствии карты в Card Management транзакция отклоняется |
| **Вид** | Негативный |
| **Предусловия** | Card Management Service доступен; карта с указанным PAN отсутствует |
| **Тестовые данные** | Несуществующий PAN |
| **Шаги** | 1. Отправить запрос авторизации с несуществующим PAN 2. Получить ответ сервиса. |
| **Ожидаемый результат** | Транзакция отклонена |

| Поле | Значение |
|-|-|
| **ID** | TC-03 |
| **Название** | Авторизация карты со статусом INACTIVE |
| **Связанное требование** | Authorization - карты со статусом INACTIVE не участвуют в операциях |
| **Вид** | Негативный |
| **Предусловия** | В системе существует карта со статусом INACTIVE |
| **Тестовые данные** | PAN карты со статусом INACTIVE |
| **Шаги** | 1. Отправить запрос авторизации 2. Получить ответ сервиса. |
| **Ожидаемый результат** | Транзакция отклонена |

| Поле | Значение |
|-|-|
| **ID** | TC-04 |
| **Название** | Авторизация карты со статусом BLOCKED |
| **Связанное требование** | Authorization - карты со статусом BLOCKED не участвуют в операциях |
| **Вид** | Негативный |
| **Предусловия** | В системе существует карта со статусом BLOCKED |
| **Тестовые данные** | PAN карты со статусом BLOCKED |
| **Шаги** | 1. Отправить запрос авторизации. |
| **Ожидаемый результат** | Транзакция отклонена|

| Поле | Значение |
|-|-|
| **ID** | TC-05 |
| **Название** | Авторизация карты с истекшим expiryDate |
| **Связанное требование** | Authorization - карта с истекшим сроком действия отклоняется |
| **Вид** | Негативный |
| **Предусловия** | В системе существует карта с истекшим сроком действия |
| **Тестовые данные** | PAN карты; expiryDate меньше текущей даты |
| **Шаги** | 1. Отправить запрос авторизации. |
| **Ожидаемый результат** | Транзакция отклонена |

| Поле | Значение |
|-|-|
| **ID** | TC-06 |
| **Название** | Авторизация при превышении dailyLimit |
| **Связанное требование** | Authorization - сумма операций за день не должна превышать dailyLimit |
| **Вид** | Негативный |
| **Предусловия** | Карта активна; баланс достаточный; сумма операции превышает дневной лимит |
| **Тестовые данные** | dailyLimit=10000; использовано за день=9000; amount=1001 |
| **Шаги** | 1. Отправить запрос авторизации. |
| **Ожидаемый результат** | Транзакция отклонена |

| Поле | Значение |
|-|-|
| **ID** | TC-07 |
| **Название** | Авторизация при превышении monthlyLimit |
| **Связанное требование** | Authorization - сумма операций за месяц не должна превышать monthlyLimit |
| **Вид** | Негативный |
| **Предусловия** | Карта активна; баланс достаточный; месячный лимит превышается |
| **Тестовые данные** | monthlyLimit=100000; использовано=99000; amount=1001 |
| **Шаги** | 1. Отправить запрос авторизации. |
| **Ожидаемый результат** | Транзакция отклонена |

| Поле | Значение |
|-|-|
| **ID** | TC-08 |
| **Название** | Авторизация при недостаточном балансе |
| **Связанное требование** | Authorization - сумма транзакции не должна превышать availableBalance |
| **Вид** | Негативный |
| **Предусловия** | Карта активна; лимиты не превышены; баланс меньше суммы операции |
| **Тестовые данные** | availableBalance=5000.00; amount=5001.00 |
| **Шаги** | 1. Отправить запрос авторизации. |
| **Ожидаемый результат** | Транзакция отклонена |

## Card Management Service

### Классы эквивалентности

**1. Создание карты**

**Поле BIN**

Позитивный класс:
- BIN состоит из 6 цифр и соответствует допустимому формату

Негативные классы:
- BIN содержит < 6 цифр
- BIN содержит > 6 цифр
- BIN содержит нецифровые символы

**Поле PAN**

Позитивный класс:
- PAN имеет корректную длину и проходит проверку алгоритмом луна

Негативные классы:
- PAN имеет некорректную длину
- PAN содержит неверную контрольную цифру

**Поле expiryDate**

Позитивный класс:
- expiryDate соответствует формату MMYY и содержит будущую дату

Негативные классы:
- expiryDate имеет неверный формат
- expiryDate содержит прошедшую дату
  
**Поле: cardholderName**

Позитивный класс:
- Имя держателя карты заполнено корректно

Негативные классы:
- Имя держателя карты отсутствует
- Имя держателя карты содержит недопустимые символы

**Поле лимиты карты**

Позитивный класс:
- dailyLimit и monthlyLimit имеют допустимые значения

Негативные классы:
- dailyLimit имеет отрицательное значение
- monthlyLimit имеет отрицательное значение
- Лимиты не переданы при создании карты

**2. Получение карты**

Позитивный класс:
- PAN существует в БД

Негативные классы:
- PAN отсутствует в БД
- PAN имеет неверный формат

**3. Получение списка карт**

Позитивные классы:
- limit и offset имеют допустимые значения
- Передан существующий статус карты для фильтрации
- Передан существующий BIN для фильтрации

Негативные классы:
- limit имеет отрицательное значение
- offset имеет отрицательное значение
- Передан неизвестный статус карты
- Передан несуществующий BIN

**4. Обновление карты**

Позитивные классы:
- PAN существует в системе
- Передано корректное значение изменяемого поля

Негативные классы:
- PAN отсутствует в системе
- Передано некорректное значение статуса
- Передано отрицательное значение лимита или баланса

**5. Удаление карты**

Позитивный класс:
- PAN существующей карты переводится в статус DELETED

Негативные классы:
- PAN отсутствует в системе

**6. Генерация тестовых карт**

**Поле count**

Позитивный класс:
- count содержит допустимое количество карт для генерации

Негативные классы:
- count = 0.
- count < 0
- count отсутствует

**Поле BIN**

Позитивный класс:
- Передан список корректных BIN длиной 6 цифр

Негативные классы:
- Список BIN пустой
- BIN имеет неверный формат
- BIN содержит нецифровые символы

**7. Резервирование средств**

Позитивный класс:
- Сумма резервирования меньше или равна доступному балансу

Негативные классы:
- Сумма резервирования превышает доступный баланс
- PAN карты отсутствует в системе
- Сумма резервирования имеет отрицательное значение


### Анализ граничных значений
**1. PAN**

Проверяется граница длины номера карты

Граница:
- длина PAN = 16 символов

Проверяемые значения:
- PAN = 15 символов - значение ниже границы, отклонить
- PAN = 16 символов - значение на границе, принять
- PAN =17 символов - значение выше границы, отклонить

**2. Контрольная цифра PAN**

Проверяется корректность контрольной цифры номера карты

Граница:
- корректность прохождения проверки алгоритмом Луна

Проверяемые значения:
- PAN с корректной контрольной цифрой - успех
- PAN с неверной контрольной цифрой - карта не создается
  
**3. ExpiryDate**

Проверяется формат и срок действия карты

Граница:
- текущая дата окончания действия карты

Проверяемые значения:
- дата < текущей - операция отклоняется
- текущая дата - успех
- дата > текущей - успех

**4. Count при генерации карт**

Проверяется количество создаваемых карт при использовании POST /api/cards/generate

Граница:
- минимально допустимое количество создаваемых карт

Проверяемые значения:
- count = 0 - значение ниже допустимого, генерация не выполняется.
- count = 1 - минимальное допустимое значение, создается одна карта.
- count > 1 - создается указанное количество карт.

### Тест-кейсы
| Поле | Значение |
|-|-|
| **ID** | TC-09 |
| **Название** | Успешное создание новой карты |
| **Связанное требование** | Card Management - при создании карты генерируется PAN, expiryDate, назначается статус ACTIVE и карта сохраняется в PostgreSQL |
| **Источник** | Класс эквивалентности: корректный BIN, корректные данные карты |
| **Вид** | Позитивный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | Card Management Service доступен; база данных доступна |
| **Тестовые данные** | BIN = 400000; cardholderName = VIKA AL; currencyCode = 643; dailyLimit = 15000000; monthlyLimit = 300000000 |
| **Шаги** | 1. Отправить запрос POST /api/cards. 2. Получить ответ сервиса. |
| **Ожидаемый результат** | Карта создана; сгенерирован PAN; PAN проходит проверку Луна; expiryDate соответствует формату MMYY; статус карты ACTIVE; карта сохранена в БД |
| **Постусловие** | В базе данных появляется новая карта |

| Поле | Значение |
|-|-|
| **ID** | TC-10 |
| **Название** | Создание карты с BIN длиной менее 6 символов |
| **Связанное требование** | Card Management - BIN карты должен состоять из 6 цифр |
| **Источник** | Класс эквивалентности: BIN содержит менее 6 символов |
| **Вид** | Негативный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | Card Management Service доступен |
| **Тестовые данные** | BIN = 4000 |
| **Шаги** | 1. Отправить запрос POST /api/cards с BIN длиной менее 6 символов |
| **Ожидаемый результат** | Карта не создается, сервис возвращает ошибку валидации |
| **Постусловие** | Новая карта отсутствует в БД |

| Поле | Значение |
|-|-|
| **ID** | TC-11 |
| **Название** | Создание карты с BIN, содержащим нецифровые символы |
| **Связанное требование** | Card Management - BIN карты должен содержать только цифры |
| **Источник** | Класс эквивалентности: BIN содержит нецифровые символы |
| **Вид** | Негативный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | Card Management Service доступен |
| **Тестовые данные** | BIN = ABCDEF |
| **Шаги** | 1. Отправить запрос POST /api/cards с BIN, содержащим буквы |
| **Ожидаемый результат** | Карта не создается, сервис возвращает ошибку валидации |
| **Постусловие** | Новая карта отсутствует в БД |

| Поле | Значение |
|-|-|
| **ID** | TC-12 |
| **Название** | Получение карты по существующему PAN |
| **Связанное требование** | Card Management - GET /api/cards/{pan} возвращает карту по PAN |
| **Источник** | Класс эквивалентности: PAN существует в системе |
| **Вид** | Позитивный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | В системе существует карта с указанным PAN |
| **Тестовые данные** | PAN существующей карты |
| **Шаги** | 1. Отправить GET /api/cards/{pan}. |
| **Ожидаемый результат** | Ответ 200; возвращены данные карты |
| **Постусловие** | Данные карты не изменены |

| Поле | Значение |
|-|-|
| **ID** | TC-13 |
| **Название** | Получение карты с отсутствующим PAN |
| **Связанное требование** | Card Management - при отсутствии карты возвращается 404 |
| **Источник** | Класс эквивалентности: PAN отсутствует в системе |
| **Вид** | Негативный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | Card Management Service доступен; карта отсутствует в БД |
| **Тестовые данные** | Несуществующий PAN |
| **Шаги** | 1. Отправить GET /api/cards/{pan}. |
| **Ожидаемый результат** | Ответ 404; карта не найдена |
| **Постусловие** | Данные системы не изменены |

| Поле | Значение |
|-|-|
| **ID** | TC-14 |
| **Название** | Частичное обновление данных карты |
| **Связанное требование** | Card Management - PATCH /api/cards/{pan} изменяет переданные поля карты |
| **Источник** | Класс эквивалентности: существующий PAN и корректные значения полей |
| **Вид** | Позитивный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | В системе существует карта |
| **Тестовые данные** | PAN существующей карты; новый dailyLimit |
| **Шаги** | 1. Отправить PATCH запрос с изменяемым полем 2. Получить ответ сервиса. |
| **Ожидаемый результат** | Карта обновлена; изменено только переданное поле |
| **Постусловие** | Новые значения сохранены в БД |

| Поле | Значение |
|-|-|
| **ID** | TC-15 |
| **Название** | Удаление существующей карты |
| **Связанное требование** | Card Management - DELETE выполняет мягкое удаление путем изменения статуса на DELETED |
| **Источник** | Класс эквивалентности: существующий PAN |
| **Вид** | Позитивный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | В системе существует карта |
| **Тестовые данные** | PAN существующей карты |
| **Шаги** | 1. Отправить DELETE /api/cards/{pan}. |
| **Ожидаемый результат** | Статус карты изменен на DELETED; карта не возвращается в GET /api/cards |
| **Постусловие** | Карта остается в БД со статусом DELETED |

| Поле | Значение |
|-|-|
| **ID** | TC-16 |
| **Название** | Генерация тестовых карт |
| **Связанное требование** | Card Management - POST /api/cards/generate создает указанное количество тестовых карт |
| **Источник** | Класс эквивалентности: корректный count и список BIN |
| **Вид** | Позитивный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | Card Management Service доступен |
| **Тестовые данные** | count = 100; bins = ["400000", "400001"] |
| **Шаги** | 1. Отправить запрос генерации карт 2. Получить ответ сервиса. |
| **Ожидаемый результат** | Создано указанное количество карт; карты имеют корректные PAN, статус и баланс |
| **Постусловие** | В БД добавлены новые карты |

| Поле | Значение |
|-|-|
| **ID** | TC-17 |
| **Название** | Успешное резервирование средств |
| **Связанное требование** | Card Management - POST /api/cards/{pan}/reserve уменьшает availableBalance карты |
| **Источник** | Класс эквивалентности: сумма резервирования меньше или равна балансу |
| **Вид** | Позитивный |
| **Уровень** | Низкоуровневый |
| **Предусловия** | Карта существует; баланс достаточный |
| **Тестовые данные** | PAN существующей карты; amount = 1000 |
| **Шаги** | 1. Отправить запрос резервирования средств. |
| **Ожидаемый результат** | Ответ 200; availableBalance уменьшен на сумму операции |
| **Постусловие** | Новый баланс сохранен в БД |

## PICT

В модель PICT я передала параметры и значения сервиса, добавила ограничения. В результате PICT сгенерировал сокращенный набор комбинаций (cases.txt и cases-card-management.txt), покрывающий все пары параметров, каждая строка которого соответствует отдельному тест-кейсу.

